import React from 'react';
import Navigation from './Navigation';

const Layout = ({children}) => {
    return (
        <>
        <div>
            {/* <Navigation /> */}
            
            abc
        </div>
        <main>{children}</main>
        </>
    );
};

export default Layout;